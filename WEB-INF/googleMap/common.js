// Mapを作成
function createMap(opts){
    var marker = new google.maps.Marker(opts);
    return marker;
}

// マーカーを作成
function createMarker(opts){
    var marker = new google.maps.Marker(opts);
    return marker;
}